---
import { cn } from '@/utils/styles';

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<article class={cn(
  "h-fit bg-charcoal-gray border border-cultured rounded-4xl p-4 transition-all duration-300",
  className
)}>
  <slot />
</article>